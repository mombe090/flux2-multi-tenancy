apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: nginxmombe090
  namespace: apps
spec:
  serviceAccountName: dev-team
  releaseName: nginxer
  chart:
    spec:
      chart: nginx
      sourceRef:
        kind: HelmRepository
        name: mombe090
      version: ">=2.5.2  <3.0.0"
  interval: 30s
